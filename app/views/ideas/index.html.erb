<h1>Nikki's Neat Ideas</h1>

<p>A few things to try one day.</p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Picture</th>
      <th>Show</th>
      <th>Edit</th>
      <th>Destroy</th>
    </tr>
  </thead>

  <tbody>
    <% @ideas.each do |idea| %>
      <tr>
        <td><%= idea.name %></td>
        <td><%= idea.description %></td>
        <td><%= idea.picture %></td>
        <td><%= link_to 'x', idea %></td>
        <td><%= link_to 'x', edit_idea_path(idea) %></td>
        <td><%= link_to 'x', idea, method: :delete, data: { confirm: 'Uh, really? Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Idea', new_idea_path %>
